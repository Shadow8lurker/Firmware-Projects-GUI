{
  "name": "@commwatch/decoders",
  "version": "0.1.0",
  "description": "Protocol decoders for CommWatch",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test src/index.test.ts"
  },
  "dependencies": {
    "@commwatch/proto-core": "workspace:*"
  }
}
