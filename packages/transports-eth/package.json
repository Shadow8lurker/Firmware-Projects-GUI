{
  "name": "@commwatch/transports-eth",
  "version": "0.1.0",
  "description": "CommWatch Ethernet transport adapter",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test src/sim.test.ts"
  },
  "dependencies": {
    "@commwatch/proto-core": "workspace:*"
  }
}
