{
  "name": "@commwatch/transports-i2c",
  "version": "0.1.0",
  "description": "CommWatch I2C transport adapter",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test src/sim.test.ts"
  },
  "dependencies": {
    "@commwatch/proto-core": "workspace:*"
  }
}
