{
  "name": "@commwatch/ui-kit",
  "version": "0.1.0",
  "description": "Shared UI utilities for CommWatch",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test src/index.test.ts"
  },
  "dependencies": {
    "@commwatch/proto-core": "workspace:*"
  }
}
